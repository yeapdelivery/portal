/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "lh3.googleusercontent.com",
      "res.cloudinary.com",
      "s.gravatar.com",
    ],
  },
};

module.exports = nextConfig;
